// Helper logic / constants for compatibility
export const calculateCompatibility = (s1, s2) => {
  // Mock logic - in real app, use complex astrological chart mapping
  return Math.floor(Math.random() * (100 - 60) + 60); // Random 60-100%
};
