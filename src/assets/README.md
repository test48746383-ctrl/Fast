# 📂 Assets Folder

Place your static image assets here.

Required structures:
- /tarot/ (0_fool.webp, etc.)
- /zodiac/ (aries.png, etc. - optional if using emojis)
- /backgrounds/ (cosmic-bg.jpg - optional if using CSS)

Note: The code currently uses CSS gradients and Emojis to keep the app lightweight and dependency-free for the MVP. You can replace emojis with real images by updating the Data files.
